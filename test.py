"""Ex01

    Args:
        a(float): 加数
        b(float):
            加数，
            这个参数很长，写了两行，文档会当作一行处理，所有第一行后面加了逗号

    Returns:
        float:只能写一个返回值，两个的还不会

    Examples:
        >>>
        c = my_add(1,2)
        c = my_add(1,2.3)

    Note:
        * 12345678
"""
